apiVersion: v1
kind: Service
metadata:
  name: python-app-develop
  namespace: python-app-develop
spec:
  selector:
    app: python-app-develop
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: LoadBalancer
